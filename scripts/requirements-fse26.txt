# Dependencies for the FSE'26 RCABench bridge and its tests.
#
#   fse26_convert.py      Parquet -> normalised case.json  (polars)
#   fse26_convert_tar.py  streaming conversion of the archive
#   fse26_shard.py        per-category tarballs
#
# Used by the `converter-tests` job in .github/workflows/ci.yml and by
# rcabench-data's cache build. Pinned so the converted output is reproducible:
# polars reads and writes the Parquet, so a version change can move the numbers.
polars==1.33.1
coverage==7.16.0
