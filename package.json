{
  "name": "@agentix-e/micro-kinetic-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nx run-many --target=build --all",
    "prepare": "husky",
    "build:core": "nx build @agentix-e/micro-kinetic-core",
    "build:tree": "nx build @agentix-e/micro-kinetic-tree",
    "build:cutting": "nx build @agentix-e/micro-kinetic-cutting",
    "build:noise": "nx build @agentix-e/micro-kinetic-noise",
    "build:scaling": "nx build @agentix-e/micro-kinetic-scaling",
    "build:wave": "nx build @agentix-e/micro-kinetic-wave",
    "build:kinetic": "nx build @agentix-e/micro-kinetic",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:integration": "vitest run --config integration-tests/vitest.config.ts",
    "test:all": "pnpm test && pnpm test:integration",
    "coverage": "nx run-many --target=test:coverage --all --exclude=@agentix-e/micro-kinetic-integration-tests --parallel=1 --skip-nx-cache",
    "typecheck": "nx run-many --target=typecheck --all && tsc -p tsconfig.workspace.json",
    "typecheck:src": "nx run-many --target=typecheck --all",
    "typecheck:workspace": "tsc -p tsconfig.workspace.json",
    "lint": "oxlint --config .oxlintrc.json packages/*/src/ packages/*/__tests__/ benchmarks/src/ benchmarks/__tests__/ integration-tests/src/",
    "lint:eslint": "eslint packages/*/src/ benchmarks/src/ integration-tests/src/",
    "format": "prettier --write 'packages/*/src/**/*.ts' 'packages/*/__tests__/**/*.ts' 'integration-tests/src/**/*.ts' 'benchmarks/*.ts' 'benchmarks/src/**/*.ts' 'benchmarks/__tests__/**/*.ts'",
    "format:check": "prettier --check 'packages/*/src/**/*.ts' 'packages/*/__tests__/**/*.ts' 'integration-tests/src/**/*.ts' 'benchmarks/*.ts' 'benchmarks/src/**/*.ts' 'benchmarks/__tests__/**/*.ts'",
    "clean": "nx run-many --target=clean --all",
    "benchmark": "tsx benchmarks/src/run-all.ts",
    "changeset": "changeset",
    "release": "pnpm build && changeset publish",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.0",
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "fast-check": "^3.20.0",
    "husky": "^9.1.7",
    "nx": "^20.0.0",
    "oxlint": "^0.15.0",
    "prettier": "^3.4.0",
    "prettier-plugin-organize-imports": "^4.1.0",
    "tsup": "^8.3.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
